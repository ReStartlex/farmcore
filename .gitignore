/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env*
.DS_Store
# Резервные копии заявок (пишутся в рантайме, не в git)
leads.jsonl
# Локальные заметки проекта для Claude — не публиковать
CLAUDE.md
# Локальные материалы (скрины аудита, черновики) — не публиковать
photo/
old/
*.jpg
npm-debug.log*
.vercel
